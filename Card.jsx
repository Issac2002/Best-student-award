function Card({ name }) {
  return (
    <div className="card" >
      <div className="card-contant">
          <h2 className="title">{name}</h2>
      </div>
      </div>  
  );
}

export default Card;
